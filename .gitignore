__pycache__/
*.pyc
.env
.venv/
.superpowers/
*.egg-info/
.pytest_cache/
# app2 runtime data (sqlite db, sessions, results)
data/
# local scratch
tmp_*.py
# per-machine разрешения Claude Code (локальные пути, SSH-команды) — не общий конфиг
.claude/settings.local.json
# model-eval artifacts (decks, PNGs, reports)
scripts/eval_out/
# macOS Finder metadata
.DS_Store
**/.DS_Store

# Крупные раздаваемые бинарники (см. settings.downloads_dir) — кладутся scp-ом
downloads/
