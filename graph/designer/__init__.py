"""The /design from-scratch designer skill — standalone LangGraph.

Fully isolated from the donor /verstai graph: own nodes, own roles, own
prompts. Reuses only the upstream parse/brief/classify nodes (read-only).
"""
